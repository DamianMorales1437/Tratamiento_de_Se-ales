function [H] = convm (h, N)
%Create convolution matrix
%Sintax:
%  H = convm(h, N);
%Inputs:
%  h,  impulse response of the system
%  N,  length of input signal


%Edit here %%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
end
